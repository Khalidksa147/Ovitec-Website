# Block direct web access to live product data (served only via /api/products.php).
Require all denied

# Demo seed only — used as a static fallback when the API is unavailable.
<Files "products.example.json">
  Require all granted
</Files>
