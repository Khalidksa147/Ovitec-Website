# Block direct web access to live product data and PHP sessions.
Require all denied

# Demo seed only — used as a static fallback when the API is unavailable.
<Files "products.example.json">
  Require all granted
</Files>
